version: '3.8'

services:
  multi-agent-system:
    build: .
    container_name: multi-agent-system
    ports:
      - "8000:8000"    
      - "8501:8501"
    volumes:
      - .:/app
    env_file:
      - .env
    command: /start.sh
